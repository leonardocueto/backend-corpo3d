# Dependencias para el contenedor Alpine (musl).
# Diferencias con requirements.txt (dev local):
#  - psycopg[binary]: trae libpq embebida en el wheel (hay wheels musllinux), asi
#    no depende de la libpq del sistema ni de ctypes.find_library (roto en musl).
#  - uvicorn SIN [standard]: dentro del contenedor no usamos --reload/watchfiles
#    (que en musl requieren Rust). uvloop/httptools son opcionales de performance.
fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2
passlib[bcrypt]>=1.7.4
bcrypt<4.1
pydantic-settings>=2.4
pydantic[email]>=2.9
slowapi>=0.1.9
httpx>=0.27
boto3>=1.34
